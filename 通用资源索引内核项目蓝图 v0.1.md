# 通用资源索引内核项目蓝图 v0.1

> 状态：Planning / 禁止进入实现
> 当前阶段：Architecture Discovery
> 原则：先验证已有方案，再决定复用、改造还是自研
> 暂定项目代号：IndexCore（最终名称后定）

---

# 一、项目为什么存在

当前 CloudSite 1.0.0 可以正常使用。

1.0 之后的问题不是“缺几个功能”，而是索引、资源、搜索、Provider、业务模块逐渐互相耦合，导致：

* 修改索引影响网站；
* 修改网站影响索引；
* Provider 逻辑进入业务层；
* Search 与资源事实绑定；
* Index V2 无法独立验证；
* 新功能不断扩大架构边界；
* 最终很难判断谁才是真正的数据事实源。

因此本项目不再定义为：

> CloudSite 2.0 的 Index V2 模块。

重新定义为：

> 一个独立、通用、可嵌入、可服务化的资源索引核心。

CloudSite 未来只是它的一个使用者。

---

# 二、项目最终解决什么问题

输入可以来自：

* AList；
* OpenList；
* WebDAV；
* S3；
* rclone；
* 本地文件系统；
* 远程 Index Server；
* 预生成 index.zip；
* scan.list；
* 其它第三方资源目录。

统一转换为：

```text
Resource Snapshot / Resource Change
```

进入核心：

```text
Snapshot / Changes
        ↓
Validation
        ↓
Identity
        ↓
Diff
        ↓
Safety Guard
        ↓
Reconcile
        ↓
Canonical Inventory
        ↓
Change Journal
```

最终给上层提供：

```text
查询资源
目录浏览
搜索数据源
资源 ID
路径
Provider 定位信息
变化事件
索引状态
```

至于：

```text
网站怎么展示
怎么下载
怎么播放
怎么分享
怎么收藏
```

全部不是 IndexCore 的职责。

---

# 三、第一原则：不要把 Scanner 当成核心

这是结合小雅之后最重要的一次调整。

以前的思路是：

```text
Provider
   ↓
Scanner
   ↓
Index V2
```

新的设计应该是：

```text
            AList Scanner ───────┐
                                 │
            WebDAV Scanner ──────┤
                                 │
            rclone Scanner ──────┤
                                 ▼
index.zip / scan.list ─────▶ Snapshot
                                 │
Remote Index Feed ───────────────┤
                                 │
Provider Native Delta ───────────┘
                                 ↓
                         Index Core
```

所以：

> Index Core 不关心 Snapshot 是怎么产生的。

Scanner 是 Adapter。

Snapshot 才是 Kernel 的正式输入。

这样才能避免以后再次被 AList 限死。

---

# 四、项目必须划成四层

## Layer 1：Source / Collector

负责“资源从哪里来”。

例如：

```text
alist-collector
openlist-collector
webdav-collector
filesystem-collector
rclone-collector
snapshot-file-reader
remote-feed-client
```

职责：

```text
读取外部资源
↓
生成标准 Snapshot
```

不能：

* 决定最终 resource_id；
* 删除正式 Inventory；
* 修改 Search；
* 维护业务数据。

---

## Layer 2：Index Kernel

这是整个项目真正需要自己掌握的部分。

包含：

```text
Snapshot Validation
Stable Identity
Reconcile
Safety Guard
Canonical Inventory
Root Lifecycle
Change Journal
Crash Recovery
```

这层必须：

* 无 UI；
* 无 FastAPI 强依赖；
* 无 CloudSite；
* 无 AList；
* 无 Search；
* 无 Catalog。

---

## Layer 3：Storage

负责持久化。

第一阶段直接采用：

```text
PostgreSQL
```

不再采用 SQLite-first 作为正式 PoC / MVP 持久化路线。

未来如需支持其它数据库或存储实现，必须通过：

```text
Store Interface
```

进行替换，不能让 Kernel Domain 直接依赖 PostgreSQL 细节。

Kernel 只能依赖 Store Interface。

不能把 ORM / PostgreSQL schema 当 Domain。

---

## Layer 4：Consumers

消费者包括：

```text
CloudSite
Search Engine
Catalog
Media Server
管理后台
其它网站
```

它们读取：

```text
Query API
Change Journal
```

不能反过来控制 Kernel 内部状态。

---

# 五、Canonical Inventory 是唯一资源事实

必须明确：

> Index Core 的 Inventory 是资源世界的 Canonical Truth。

以后不能再同时出现：

```text
Indexing 有一份资源
Resources 有一份资源
Search 有一份资源
Catalog 又有一份资源
```

正确结构：

```text
          Canonical Inventory
             /      |      \
            /       |       \
        Search   CloudSite  Catalog
```

Search 是 Projection。

Catalog 是业务映射。

CloudSite 是应用。

---

# 六、第一阶段标准数据模型

现在先不定数据库表，只定 Domain。

## ResourceRoot

```text
root_id
source_id
source_type
source_ref
generation
state
```

---

## Snapshot

```text
snapshot_id
root_id
source
created_at
complete
entries[]
metadata
```

---

## SnapshotEntry

至少包含：

```text
provider_object_id optional
parent_ref
path
name
is_dir
size
mtime
hash optional
metadata
```

第一阶段绝对不能要求所有 Provider 都有 hash。

---

## CanonicalResource

```text
resource_id

root_id
provider_object_id optional

parent_id
path
name

is_dir
size
mtime
hash optional

state
generation

created_at
updated_at
```

---

## ChangeRecord

```text
sequence

root_id
resource_id

type

before
after

generation
committed_at
```

变化类型：

```text
add
update
rename
move
remove
conflict
```

---

# 七、Stable Identity 暂时只定目标，不提前定算法

必须支持：

```text
A.mkv
→ B.mkv
```

尽可能：

```text
same resource_id
```

以及：

```text
/电影/A.mkv
→ /归档/A.mkv
```

尽可能：

```text
same resource_id
```

但目前不提前决定：

```text
hash 算法
fingerprint 权重
matching score
```

这部分必须等 Provider 调研完成后再定。

优先级原则：

```text
Provider 原生稳定 ID
        ↓
可靠历史 Identity
        ↓
强 Fingerprint
        ↓
受控启发式匹配
        ↓
无法确定 = conflict
```

绝对不能：

```text
猜错了也自动绑定
```

---

# 八、删除必须极度保守

必须延续 CloudSite Index V2 已经确定的原则：

```text
没扫到
≠
已经删除
```

第一阶段正式定义：

```text
Missing
↓
Removal Candidate
↓
Validation
↓
Confirmed Removed
```

而不是：

```text
Missing
↓
DELETE
```

需要研究小雅的：

```text
数量差异检查
purge skip
回收站
```

结合 V2：

```text
Complete Gate
Unexpected Shrink Guard
Provider Error Guard
```

最终决定删除模型。

---

# 九、Snapshot 必须成为一等公民

这是本项目和旧 CloudSite V2 最大的结构区别之一。

Snapshot 不只是内存对象。

需要支持：

```text
生成
保存
导入
导出
校验
版本
比较
重放
```

未来理想格式：

```text
index.snapshot
```

可以：

```text
AList Collector
     ↓
index.snapshot
     ↓
IndexCore
```

也可以：

```text
远端机器
↓
生成 snapshot
↓
分发
↓
另一台服务器导入
```

这就是从小雅 `index.zip` 学到的核心思想。

---

# 十、第一阶段不要急着做 Incremental

这是一个重要约束。

开发顺序绝对不是：

```text
先解决增量
```

而是：

```text
完整 Snapshot
↓
正确 Inventory
↓
安全 Reconcile
↓
Stable Identity
↓
Crash Safety
```

全部稳定后，再研究：

```text
Delta
Dirty Scope
Rolling Verification
Remote Change Feed
```

因为错误的增量比慢一点的全量危险得多。

---

# 十一、现阶段需要研究的外部项目

不再继续无限找项目。

暂定只研究 6 个。

## A. 小雅生态

研究：

```text
index.zip
/index/*.txt
scan.list.gz
xiaoya-alist-search
xiaoya_db
xiaoya_emd_go
```

重点：

```text
预生成索引
索引分发
Diff
安全删除
并发
远程更新
```

---

## B. OpenList / AList

研究：

```text
Provider abstraction
search_nodes
database index
auto index update
302/WebDAV
```

重点不是复制整个 AList。

而是搞清楚：

```text
它如何表示目录和资源
它如何搜索
它如何挂多个 Storage
它如何把搜索结果重新定位到资源
```

---

## C. rclone

研究：

```text
Provider abstraction
backend capabilities
stable object information
remote control/API
```

重点回答：

> 我们有没有必要自己实现几十种 Provider？

---

## D. FSCrawler

研究：

```text
checkpoint
restart
crawl state
incremental model
```

---

## E. Diskover

研究：

```text
scanner/plugin architecture
index pipeline
large filesystem handling
```

---

## F. CloudSite Index V2

保留并重新评估：

```text
Concurrent Scanner
Durable Scan
Checkpoint
Complete Gate
Stable Identity
Atomic Reconcile
Dirty Scope
Rolling Verification
Audit
Fault Tests
```

必须判断：

```text
哪些是真正必要的
哪些是后来架构膨胀产生的
```

---

# 十二、研究阶段禁止复制代码

第一阶段：

> 只看，不搬。

每个 donor 都要产出一张卡：

```text
项目：
许可证：

解决的问题：

核心数据模型：

核心流程：

值得复用：

值得借鉴：

不适合：

与 IndexCore 的关系：

代码可否直接复制：
```

只有完成许可证确认后才能进入：

```text
COPY
ADAPT
REWRITE
IGNORE
```

四种分类。

---

# 十三、许可证规则提前钉死

未来 IndexCore 最好保持：

```text
Apache-2.0
或
MIT
```

因此 donor 分类：

### Apache / MIT

可以研究后直接复用部分代码。

但：

* 保留 copyright；
* 保留 LICENSE；
* 标明修改。

### GPL

优先：

```text
Clean-room rewrite
```

即：

```text
研究行为
→ 写规格
→ 写测试
→ 独立实现
```

避免把整个 Kernel 变成 GPL，除非以后明确决定项目就使用 GPL。

### 无 LICENSE

视为：

```text
不可复制
```

只研究思路。

### NonCommercial

不作为代码底座。

---

# 十四、Discovery 阶段要回答的 12 个问题

在任何代码开始前，必须回答：

1. 小雅的 `index.zip` 到底包含什么？
2. 它是谁生成的？
3. 完整索引如何更新？
4. 客户端是否需要遍历 Provider？
5. AList `x_search_nodes` 的真实语义是什么？
6. OpenList 当前数据库索引如何维护？
7. rclone 是否能提供足够统一的 Provider metadata？
8. 哪些 Provider 有稳定 object ID？
9. Snapshot 最少字段到底应该有哪些？
10. Stable Identity 第一版能做到什么程度？
11. Canonical Inventory 应该如何持久化？
12. 我们到底还剩哪些功能是真正需要自己开发的？

这 12 个问题没有答完：

> 禁止建正式 IndexCore。

---

# 十五、PoC 阶段只验证三件事

Discovery 之后，才进入 PoC。

PoC 不做：

```text
完整 Kernel
UI
后台
账号系统
CloudSite 接入
复杂 Scheduler
```

只验证：

## PoC-1：Snapshot Import

```text
index.zip / fixture
↓
标准 Snapshot
↓
PostgreSQL Inventory
```

要求：

```text
100k+
资源可以导入
路径树正确
查询正确
```

---

## PoC-2：Reconcile

输入：

```text
Snapshot V1
Snapshot V2
```

输出：

```text
Added
Changed
Moved
Renamed
Removed Candidate
```

先不要求所有 rename 都识别成功。

必须保证：

```text
绝不误删
```

---

## PoC-3：AList Resolver

```text
Inventory Resource
↓
source path
↓
AList
↓
302 / WebDAV / raw URL
```

只要这三件事成立：

> 项目方向成立。

否则重新设计。

---

# 十六、正式 MVP 只做这些

PoC 成功后，MVP：

```text
Snapshot Import
AList Collector
PostgreSQL Canonical Inventory
Root
Stable Identity v1
Safe Reconcile
Change Journal
Query API
Basic Status
Crash-safe transaction
```

不做：

```text
Search Engine
AI
Catalog
UI
用户系统
分享
收藏
下载器
115 云下载
媒体刮削
```

这些全部属于消费者层。

---

# 十七、MVP 的运行模型

第一版：

```text
Create Root
   ↓
Generate Snapshot
   ↓
Validate
   ↓
Reconcile
   ↓
Commit Inventory
   ↓
Append Change Journal
   ↓
READY
```

第二次：

```text
Generate Snapshot V2
   ↓
Compare with Inventory
   ↓
Safety Guard
   ↓
Reconcile
```

先把这个模型做正确。

---

# 十八、第二阶段再解决 Scanner Resume

只有 MVP 稳定后才做：

```text
Durable Scanner
Checkpoint
Resume
Bounded Concurrency
```

原因：

Snapshot 已经是 Kernel 输入。

所以即使 Scanner 暂时很简单：

```text
AList
↓
完整扫描
↓
snapshot
```

也不会污染 Kernel。

Scanner 将来可以独立升级。

---

# 十九、第三阶段才研究真正 Incremental

后续：

```text
Known Changes
Provider Delta
Dirty Scope
Rolling Verification
Remote Feed
```

统一输出：

```text
Change Set
```

然后走同一个：

```text
Validate
↓
Identity
↓
Safety
↓
Reconcile
```

不能出现两套资源提交机制。

---

# 二十、CloudSite 回退策略与本项目分开

这是两个项目。

## CloudSite

回到：

```text
v1.0.0
```

先保持：

```text
能跑
稳定
可部署
```

## IndexCore

独立研究。

当 IndexCore MVP 真正通过后：

```text
CloudSite 1.x
      ↓ Adapter
IndexCore
```

替换原索引。

禁止：

```text
一边做新 IndexCore
一边继续大改 CloudSite
```

这样又会重蹈 2.0。

---

# 二十一、阶段门禁

## Gate 0 — Discovery

必须得到：

```text
现有方案调查报告
许可证报告
数据模型比较
最终 Build / Buy / Adapt 决策
```

---

## Gate 1 — Architecture

必须得到：

```text
Domain Model
Snapshot Contract
Provider Contract
Store Contract
Query Contract
Failure Model
```

此时才允许建仓库。

---

## Gate 2 — PoC

必须证明：

```text
Snapshot → Inventory
Diff → Reconcile
Inventory → AList Resource
```

---

## Gate 3 — MVP

必须证明：

```text
真实 AList
真实大量资源
稳定重复索引
无误删
重启安全
```

---

## Gate 4 — CloudSite Integration

才允许：

```text
CloudSite v1
↓
IndexCore
```

---

# 二十二、当前立即停止的工作

现在暂停：

```text
新建 IndexCore 仓库
从 CloudSite 拆代码
复制小雅源码
改 main
回退 main
写新的数据库 migration
写新的 Scanner
```

这些都太早。

---

# 二十三、下一步唯一任务

下一阶段只做：

> Discovery 01 — 小雅索引链路调查。

必须搞清楚：

```text
真实数据
↓
谁生成索引
↓
index.zip
↓
/index/*.txt
↓
xiaoya-alist-search
↓
x_search_nodes
↓
AList Search
↓
资源访问
```

最终输出：

```text
XIAOYA-INDEX-ARCHITECTURE-REPORT.md
```

只写调查结果，不改任何代码。

完成后再进入：

```text
Discovery 02 — OpenList/AList Index
```

一个一个来。

---

# 二十四、项目总原则

以后所有开发先问：

> 这是 IndexCore 必须解决的问题吗？

如果不是：

```text
放到 Adapter
放到 Consumer
或者不做
```

IndexCore 最终必须尽可能小：

```text
Snapshot
Identity
Inventory
Reconcile
Safety
Journal
```

越小，越可靠。

最终希望做到：

```text
CloudSite 可以废掉重写，
AList 可以换成 OpenList，
PostgreSQL 未来也可以通过 Store Interface 替换为其它存储实现，
Search 可以换任何引擎，

但 Canonical Resource Truth 不需要重做。
```

这才是这个项目值得独立出来的原因。
